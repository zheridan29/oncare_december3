# Unit Testing — Test Case Table (Mapped to Functional Requirements)

Headers required by user: **Test Case, Test Level, Target Module, Test Description, Expected Outcome, Results**  
Additional column: **FR Reference**

| Test Case | Test Level | Target Module | FR Reference | Test Description | Expected Outcome | Results |
|---|---|---|---|---|---|---|
| Admin Create User Account Validation | Unit Test | `accounts` | FR-1.1.1 | Validate user creation logic with required fields (username/email/password/role) and invalid/missing data. | Valid data passes; invalid data is rejected with clear validation errors; account is not created on invalid input. |  |
| Admin Modify User Role Validation | Unit Test | `accounts` | FR-1.1.2 | Validate role assignment logic (Sales Rep/Pharmacist-Admin/System Admin) and disallow unknown roles. | Only supported roles are accepted; unsupported role values are rejected. |  |
| User Activity Log Record Creation | Unit Test | `audits` | FR-1.1.3 | Create an activity log entry for a user action (e.g., login) using audit logging API/model. | Activity log entry persists with correct user, action type, and timestamp. |  |
| Audit Log Entry Structure for Critical Operations | Unit Test | `audits` | FR-1.3.1 | Create audit log entries for order, payment, inventory, prescription actions and verify required fields are present. | Each critical operation produces an audit entry with actor, action, object reference, and timestamp. |  |
| Audit Log Filtering Parameters Validation | Unit Test | `audits` | FR-1.3.2 | Validate audit log filter inputs (date range, user, action type) before query execution. | Filter inputs are accepted when valid; invalid filters are rejected/normalized safely. |  |
| Audit Log Retention Rule Configuration | Unit Test | `audits` | FR-1.3.3 | Verify retention period configuration values are valid and used by cleanup/retention logic (if implemented). | Retention setting is valid and applied consistently by retention logic. |  |
| Registration Field Requirements | Unit Test | `accounts` | FR-2.1.1 | Validate registration form requires email, username, password, and role selection. | Missing required fields produce errors; valid data passes. |  |
| Secure Login Credential Validation | Unit Test | `accounts` | FR-2.1.2 | Validate authentication accepts correct username/email + password and rejects incorrect credentials. | Correct credentials authenticate; wrong credentials fail without leaking sensitive details. |  |
| Password Strength Rules | Unit Test | `accounts` | FR-2.1.3 | Submit weak passwords (short, missing uppercase/lowercase/number/special) and a strong password. | Weak passwords are rejected; strong password is accepted. |  |
| Password Reset Token Validation | Unit Test | `accounts` | FR-2.1.4 | Validate password reset token generation/validation workflow (token format, expiry, one-time usage). | Valid token resets password; invalid/expired token is rejected. |  |
| Session Timeout Configuration | Unit Test | `accounts` | FR-2.1.5 | Validate session timeout value (30 minutes inactivity) is configured and interpreted correctly by session logic. | Session timeout value equals required policy and triggers logout after inactivity. |  |
| RBAC Role Enumeration | Unit Test | `accounts` | FR-2.2.1 | Verify the system defines exactly three roles and uses them consistently in permission checks. | Only the three roles exist; checks map correctly to each role. |  |
| RBAC Feature Access Matrix | Unit Test | `accounts` | FR-2.2.2 | Validate permission rules for Sales Rep, Pharmacist/Admin, System Admin access sets (logical checks). | Role-to-feature checks return expected true/false. |  |
| Role-Specific Dashboard Routing Rules | Unit Test | `accounts` | FR-2.2.3 | Validate post-login redirect selection logic based on role. | Correct dashboard URL is selected for each role. |  |
| Unauthorized Access Handling Message | Unit Test | `accounts` | FR-2.2.4 | Validate the system produces a consistent “access denied” response/message when permission checks fail. | Access denied produces correct status/redirect and message payload. |  |
| Profile Update Validation | Unit Test | `accounts` | FR-2.3.1 | Validate profile updates require valid email/contact formats and save changes. | Valid updates persist; invalid values rejected with errors. |  |
| Profile Change History Record | Unit Test | `audits` | FR-2.3.2 | Verify a profile change produces a history/audit entry with old/new values (if tracked). | Profile history/audit record exists and is linked to user. |  |
| Medicine Create Field Validation | Unit Test | `inventory` | FR-3.1.1 | Validate Medicine creation requires fields such as name, category, manufacturer, unit_price, units_per_box, stock, reorder point, prescription flag. | Valid medicine saves; missing/invalid fields are rejected. |  |
| Medicine Edit Audit Trail Entry | Unit Test | `audits` + `inventory` | FR-3.1.2 | Update a medicine and verify audit logging captures the change. | Audit record created showing editor, medicine, and changes. |  |
| Prevent Deleting Medicine with Orders | Unit Test | `inventory` + `orders` | FR-3.1.3 | Attempt to delete a medicine referenced by an existing OrderItem. | Deletion blocked; appropriate validation/error returned. |  |
| Stock Status Display Logic | Unit Test | `inventory` | FR-3.1.4 | Compute/display stock status based on current_stock and reorder_point. | In Stock/Low Stock/Out of Stock labels match rules. |  |
| Medicine Search Filter Input Validation | Unit Test | `inventory` | FR-3.1.5 | Validate medicine filter parameters (name/category/manufacturer/status/prescription-required). | Filters accept valid parameters and reject invalid ones safely. |  |
| Category CRUD Validation | Unit Test | `inventory` | FR-3.2.1 | Validate creating/editing/deleting categories with required name and constraints. | Valid operations succeed; invalid rejected. |  |
| Category Organization Rule | Unit Test | `inventory` | FR-3.2.2 | Ensure a medicine can be associated with a category and retrieved by category grouping. | Medicine-category relationship persists and is queryable. |  |
| Prevent Deleting Category with Medicines | Unit Test | `inventory` | FR-3.2.3 | Attempt to delete a category that has associated medicines. | Deletion blocked with validation/error. |  |
| Manufacturer CRUD Validation | Unit Test | `inventory` | FR-3.3.1 | Validate creating/editing/deleting manufacturer with required name and constraints. | Valid operations succeed; invalid rejected. |  |
| Manufacturer Link to Medicine | Unit Test | `inventory` | FR-3.3.2 | Ensure a medicine references a manufacturer and can be filtered/grouped by it. | Relationship persists and is queryable. |  |
| Prevent Deleting Manufacturer with Medicines | Unit Test | `inventory` | FR-3.3.3 | Attempt to delete a manufacturer that has associated medicines. | Deletion blocked with validation/error. |  |
| Inventory Level Model Consistency | Unit Test | `inventory` | FR-4.1.1 | Create medicines and verify inventory level fields are stored consistently (non-negative, numeric). | Inventory levels persist and remain valid. |  |
| Stock Update Function Validity | Unit Test | `inventory` | FR-4.1.2 | Validate stock update helper logic that increases/decreases stock and prevents invalid states. | Stock updates correctly; invalid operations are rejected. |  |
| Stock Movement Record Validation | Unit Test | `inventory` | FR-4.1.3 | Create stock movement entries and validate required fields (type, qty, reference, actor, timestamp). | Valid movements save; invalid are rejected. |  |
| Stock Movement Audit Fields Present | Unit Test | `inventory` | FR-4.1.4 | Verify stock movement record includes movement type, qty, reference, user, timestamp. | All audit-trail fields exist and are persisted. |  |
| Automatic Stock Status Classification | Unit Test | `inventory` | FR-4.1.5 | Given different combinations of `current_stock` and `reorder_point`, evaluate the computed stock status (In Stock / Low Stock / Out of Stock). | Status matches rules: In Stock (stock > reorder point), Low Stock (stock ≤ reorder point and > 0), Out of Stock (stock = 0). |  |
| Reorder Alert Generation Rule | Unit Test | `inventory` | FR-4.2.1 | For a medicine, set stock ≤ reorder_point and trigger alert generation logic. | ReorderAlert is created exactly when threshold is reached. |  |
| Cart Add Item Model Validation | Unit Test | `orders` | FR-5.1.1 | Validate cart item creation supports quantities in boxes or units and rejects invalid quantities. | Valid cart item saves; invalid rejected. |  |
| Cart Total Calculation (Boxes) | Unit Test | `orders` | FR-5.1.2 | Calculate totals for cart items measured in boxes using units_per_box and unit_price. | Total equals quantity × units_per_box × unit_price (boxes). |  |
| Cart View Data Composition | Unit Test | `orders` | FR-5.1.3 | Build cart view context and ensure it contains medicine details, qty, unit price, total price. | Context includes required fields for display. |  |
| Cart Quantity Update Validation | Unit Test | `orders` | FR-5.1.4 | Validate updating cart quantity persists and enforces non-negative constraints. | Quantity updated correctly; invalid rejected. |  |
| Cart Remove Item Behavior | Unit Test | `orders` | FR-5.1.5 | Remove a cart item and verify it is deleted and totals update. | Item removed; totals reflect removal. |  |
| Cart Clear Behavior | Unit Test | `orders` | FR-5.1.6 | Clear all cart items for a user. | Cart becomes empty; totals become zero. |  |
| Cart Count Calculation Source | Unit Test | `orders` | FR-5.1.7 | Evaluate cart total_items (badge source) for multiple items/quantities. | Returns correct sum of quantities. |  |
| Add-to-Cart Stock Validation Rule | Unit Test | `orders` + `inventory` | FR-5.1.8 | Validate cart add logic rejects quantities exceeding available stock. | Over-stock add is rejected; in-stock add succeeds. |  |
| Order Item Totals Calculation | Unit Test | `orders` | FR-6.1.4 | Validate OrderItem total calculation matches quantity × units_per_box × unit_price for boxes. | Total computed and saved correctly. |  |
| Unique Order Number Constraint | Unit Test | `orders` | FR-6.1.6 | Attempt to create two orders with same order_number (if set manually) to test uniqueness. | Duplicate blocked; order numbers remain unique. |  |
| Default Order Status on Create | Unit Test | `orders` | FR-6.1.7 | Create an order and inspect default status field. | Status defaults to Pending. |  |
| Default Payment Status and Create-Time Restriction | Unit Test | `orders` | FR-6.1.8 | Validate initial payment status is Pending and “Paid” cannot be selected at order creation form. | Payment status defaults to Pending; Paid option removed/disabled. |  |
| Supported Payment Status Enumeration | Unit Test | `orders` | FR-7.3.1 | Validate supported payment statuses are Pending, Paid, Rejected. | Only these statuses are accepted/stored. |  |
| File Upload Type and Size Validation | Unit Test | `common` | FR-13.1.2 | Validate file upload validation rejects disallowed types and oversized files. | Invalid files rejected; valid types (JPG/PNG/PDF) accepted. |  |

